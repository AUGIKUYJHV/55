<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- صلاحيات الموقع الجغرافي -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- صلاحيات التخزين لقراءة/كتابة الصور الجوية وملفات KML/KMZ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- الإنترنت (فقط عند رغبة المستخدم بتحميل صور جديدة، التطبيق يعمل أوفلاين بدونها) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- بصمة الإصبع/الوجه -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".SurveyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.YemenSurveyMap"
        tools:targetApi="34">

        <activity
            android:name=".ui.LockActivity"
            android:exported="true"
            android:theme="@style/Theme.YemenSurveyMap.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.YemenSurveyMap.NoActionBar" />

        <activity
            android:name=".ui.LayerManagerActivity"
            android:exported="false"
            android:label="@string/layers_title" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

    </application>

</manifest>
